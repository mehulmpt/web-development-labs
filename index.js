import { exec, spawn } from 'child_process'

// Create child process using exec method and run a shell command

// Print the output of the shell command in the console

// Handle errors during child process execution

// Create child process using spawn method and run a command with arguments
