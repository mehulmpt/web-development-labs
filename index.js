/**
 * @param {Function[]} functions
 * @return {Function}
 */
export const compose = function (functions) {
	return function (x) {}
}

/**
 * const fn = compose([x => x + 1, x => 2 * x])
 * fn(4) // 9
 */
