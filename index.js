/**
 * @param {number} millis
 */
export async function sleep(millis) {}

/**
 * Usage example:
 * let t = Date.now()
 * sleep(100).then(() => console.log(Date.now() - t)) // expected output: 100
 */
