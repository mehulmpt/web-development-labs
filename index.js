// Define the four variables here

// Define 'num'

// Define 'str'

// Define 'bool'

// Define 'arr'

// Click on the 'Run' button to see the values logged to the console
console.log({
	num,
	str,
	bool,
	arr,
});

// The code below this line is used for tests - don't change it
export { num, str, bool, arr };
