// Create a set named 'mySet' and add numbers 1, 2, and 3 to it.

// Define the 'union' function that takes two sets as input.

// Define the 'intersection' function that takes two sets as input.

// Export the 'union' and 'intersection' functions using ESM syntax
