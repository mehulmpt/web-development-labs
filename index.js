/**
 * @param {Array} arr
 * @return {Generator}
 */
export function* inorderTraversal(arr) {}

/**
 * const gen = inorderTraversal([1, [2, 3]]);
 * gen.next().value; // 1
 * gen.next().value; // 2
 * gen.next().value; // 3
 */
