// Use ESM imports and work with the fs and stream modules to accomplish the tasks.

// Create a Read stream and export it as readStream.

// Create a Write stream and export it as writeStream.

// Create a transform stream that converts the text to uppercase and export it as uppercaseTransformStream.

// Use stream.pipeline() to pipe readStream to uppercaseTransformStream and then writeStream.

// Handle errors in stream.pipeline().
