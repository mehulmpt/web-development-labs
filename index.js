// Start by creating an object 'person' with properties 'name', 'age', and 'city'

// Next, use object destructuring to extract the properties from 'person'

// Create a function named 'extractName' that takes an object as input and returns the 'name' property using object destructuring

// Create an array named 'numbers' and use array destructuring to extract the first three elements
