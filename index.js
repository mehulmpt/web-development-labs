/*
 * Create two promises that resolve after different delays.
 * Use Promise.race() to select the first promise to resolve.
 * Implement an async function to handle the output of Promise.race().
 */
