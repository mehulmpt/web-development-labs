class ArrayWrapper {
	constructor(nums) {
		this.nums = nums
	}

	valueOf() {
		// your implementation
	}

	toString() {
		// your implementation
	}
}

export default ArrayWrapper

/**
 * const obj1 = new ArrayWrapper([1,2]);
 * const obj2 = new ArrayWrapper([3,4]);
 * obj1 + obj2; // 10
 * String(obj1); // "[1,2]"
 * String(obj2); // "[3,4]"
 */
