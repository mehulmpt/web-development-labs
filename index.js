// Start by importing the events module and creating an instance of EventEmitter

// Next, create the eventHandler function

// Now, attach the eventHandler as a listener to the data_received event

// Finally, emit the data_received event with the payload 'Hello, EventEmitter!'
