// Create a URLSearchParams instance using the query string '?key=value&name=example'

// Get the value of the 'key' parameter and log it to the console

// Update the value of the 'key' parameter to 'newValue'

// Convert the URLSearchParams instance back to a string and log it to the console
